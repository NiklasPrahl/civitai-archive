[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --cov=civitai_manager --cov-report=html
markers = 
    slow: marks tests as slow (deselect with '-m "not slow"')
    security: security related tests
    performance: performance related tests
